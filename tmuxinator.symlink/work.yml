name: work
root: ~/
windows:
  - c:
        layout: main-vertical
        panes:
          - cd ~/source/work/tonsser/tonsser-api; git up-to-date && exit
          - cd ~/source/work/tonsser/tonsser-api; vim .
  - servers:
        layout: main-vertical
        panes:
          - elasticsearch
          - redis-server
          - cd ~/source/work/tonsser/tonsser-api; rails s
  - rails:
        layout: main-vertical
        panes:
          - cd ~/source/reference/rails; vim .
