name: tonsser
root: ~/dev
windows:
  - rails:
        layout: main-vertical
        panes:
          - cd ~/dev/major/api/; vim .
  - rust:
        layout: main-vertical
        panes:
          - cd ~/dev/major/graphql-api/; vim .
  - servers:
        panes:
          - /Users/davidpdrsn/dev/archive/elasticsearch-6.2.4/bin/elasticsearch
          - redis-server
          - cd ~/dev/major/graphql-api; cargo docserver -r "--no-deps"
  - dotfiles:
        layout: main-vertical
        panes:
          - cd ~/dotfiles
