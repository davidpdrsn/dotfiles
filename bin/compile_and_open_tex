#!/usr/bin/env ruby

path = ARGV[0]

unless path
  puts "Give me a .tex file to compile"
  exit 1
end

filename = File.basename path

if system "pdflatex #{filename}"
  system "open #{filename.sub(/\.tex$/, '.pdf')}"
end
