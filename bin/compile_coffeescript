#!/usr/bin/env ruby

require 'coffee-script'

if ARGV[0]
  filename = ARGV[0]
  compiled = CoffeeScript.compile(File.read(filename))
  File.write(filename.sub(".coffee",".js"), compiled)
  exit 0
else
  puts "Usage: compile_coffeescript <filename>"
  exit 1
end

