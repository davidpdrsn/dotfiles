#!/bin/sh

ctags --fields=+l -R --exclude=build --exclude=.svn --exclude=.git --exclude=log --exclude=tmp --exclude=*.html --exclude=vendor --exclude=*.js *

if [ -f "stack.yaml" ]
then
  codex update
fi
