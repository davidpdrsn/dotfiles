# Shortcuts
alias bin='cd ~/dotfiles/bin; l'
alias dot='cd ~/dotfiles; l'
alias dt='cd ~/desktop; l'
alias code='cd ~/code; l'
alias pro='cd ~/projects; l'
alias cabana='cd ~/work/cabana; l'
alias hax='cd ~/hax; l'

alias u='cd ~/uni; l'
alias oo='cd ~/uni/oopd; l'
alias lin='cd ~/uni/linalgdat; l'

alias v='vim'

# Edit config files
alias ea='vim ~/dotfiles/zsh/aliases'
alias ef='vim ~/dotfiles/zsh/functions'
alias ee='vim ~/dotfiles/zsh/env'
alias ep='vim ~/dotfiles/zsh/prompt'

# Utility
alias reload='source ~/.zshrc'
alias realod='source ~/.zshrc'
alias build='ruby build.rb'
alias buidl='ruby build.rb'

# Common
alias c='clear'
alias cdd='cd -'  # goto last dir cd'ed from
alias cl='clear; l'
alias cls='clear; ls'
alias h='history'
alias l.='ls -d .[^.]*'
alias l='ls -lhGt'  # -l long listing, most recent first, -G color
alias la='ls -lhGta'
alias lh="ls -lh"
alias ll='ls -lhG'  # -l long listing, human readable, no group info
alias lt='ls -lt' # sort with recently modified first
alias md='mkdir -p'
alias ..='cd ..'   # up one dir
alias mosml='rlwrap mosml -P full'
alias rakes='rake --tasks'
alias rmd='rm -rf'
alias cask='brew cask'
alias ccat='highlight -O ansi'
alias attscreen='screen -dRR'

# PostgreSQL
alias dblist='psql -l'

# Finder
alias o='open . &'

# Processes
alias cpu='top -o cpu' # cpu
alias memo='top -o vsize' # memory

# Flush DNS cache
alias flushdns="dscacheutil -flushcache"

# Don't autocorrect on these commands
alias bundle='nocorrect bundle'
alias rspec='nocorrect rspec'
alias jsc='nocorrect jsc'
alias ijs='nocorrect jsc'

