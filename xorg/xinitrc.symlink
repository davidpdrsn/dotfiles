xrdb -merge ~/.Xresources

if [ -s ~/.Xmodmap ]; then
  xmodmap ~/.Xmodmap
fi

feh --bg-scale ~/background.png &
xbindkeys &
prevent-death &
dunst &
xss-lock -- xscreensaver-command -lock &

exec xmonad
